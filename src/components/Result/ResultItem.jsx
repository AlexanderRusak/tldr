import { TabComponent } from '../TabComponent/TabComponent'

export const ResultItem = ({ shortData, longData }) => {

  return (
    <>
      <TabComponent short={shortData} long={longData} />
    </>
  )
}

